[package]
name = "aws-vpc"
authors = ["Keith Sharp <kms@passback.co.uk"]
description = "Tagging and describing VPCs."
license = "AGPL-3.0-or-later"
version = "0.1.0"
edition = "2021"

[dependencies]
aws-config = { workspace = true }
aws-sdk-ec2 = { workspace = true }
tokio = { workspace = true }